import React from 'react';

import Brand from '../brand';
import Menu from '../menu';

const Header = () => (
    <>
        <Brand />
        <Menu />
        <h1>Header</h1>
    </>
);

export default Header;
